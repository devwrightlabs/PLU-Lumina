Act as an expert Senior Web3 Solutions Architect, Lead Cryptographic Engineer, and Elite Frontend UI/UX Designer. Initialize a high-performance workspace to build "PLU-Lumina", an enterprise-grade omnichain banking terminal and liquidity bridge for the Pi Network ecosystem. This application handles 100% crypto-native assets (BTC, ETH, Pi) and manages zero custodial fiat.

### 1. Technology Stack and Non-Custodial Architecture
- **Frontend:** Next.js (App Router), TypeScript, Tailwind CSS, and Three.js/WebGL for 3D graphics and typography.
- **Backend Orchestrator:** Go (Golang) for concurrent multi-chain indexing, secure address derivation, and high-frequency data streaming.
- **Data Layer:** Supabase/PostgreSQL for secure multi-chain wallet mapping and transactional state persistence.
- **Smart Contracts (Mocked for Replit):** Soroban/Stellar Rust-based contracts executing the core non-custodial logic: `Mint()`, `Burn()`, `Stake()`, `Borrow()`, and `Liquidate()`. Integrate mocked **reflector.space** decentralized oracles for real-time asset price feeds.
- **Security:** Integrate secp256r1 passkey authentication for biometric transaction signing within the webview.

### 2. Elite Enterprise UI/UX Design (The "Dark Luxury" Terminal)
The app must not look like a basic web page. It must function as a luxury financial terminal within a mobile webview environment.
- **Color Palette:** Deep black background (#0A0A0F) with strict brushed gold highlights (#F0C040).
- **Glassmorphism & Depth:** Abandon flat CSS panels. All container cards must use frosted-glass effects (blur backdrops with 1px semi-transparent gold borders) to establish clean visual layering.
- **3D & Interactive Typography (Meta/Horizon OS Style):** Use modern, spatial typography systems similar to those used by Facebook/Meta for global CSS. Text must feel 3D, in-depth, and interactive. Implement dynamic fonts that react to hover states or data changes (e.g., numbers that morph or pulse subtly). Use Three.js to render WebGL topographic data visualizations for asset performance rather than basic 2D line charts.
- **Progressive Disclosure:** Keep the main view uncluttered. Advanced data (contract hashes, oracle feeds) should be hidden behind clean, expandable menus.

### 3. Mandatory Protocol Monetization Engine (Centralized Revenue Routing)
All protocol fees generated across the platform must be hardcoded into the contract logic and routed **directly and entirely to the single Pi Network App Developer Wallet** generated for PLU-Lumina. This ensures clean accounting for the Pi Core Team's 30% profit-sharing model. Use the following categorized fee structure:
- **Bridging Fees:** A flat percentage fee automatically deducted when a user bridges external assets into or out of the ecosystem.
- **Borrowing Fees:** An origination fee charged when a loan is opened, plus a reserve cut of the continuous interest paid by the borrower.
- **Staking Fees:** A performance fee automatically skimmed from the yield/profits generated by a user's staked assets (never touching their principal).
- **Trading & Swap Fees (Maker/Taker):** Charge a Maker fee for users providing liquidity/limit orders, and a higher Taker fee for users executing immediate market swaps. 
- **Other Service Fees:** A universal micro-fee applied to any other advanced DeFi services utilized on the platform.

### 4. Core Feature Specifications

#### A. Dynamic Interest Rate Engine (The Utilization Curve)
Implement Aave V3 logic for self-balancing liquidity pools.
- **Math Formula:** Use a two-tiered piecewise linear function.
- **Parameters:** Base Rate = 2%, Optimal Utilization Rate (Kink) = 80%, Slope 1 = 4%, Slope 2 = 60%. 
- **Action:** When liquidity utilization is <80%, apply a gentle rate curve. When utilization exceeds 80%, apply the exponential rate spike to automatically incentivize loan repayment and attract new stakers.

#### B. The Wrap/Unwrap Engine
Strictly Non-Custodial bridging mechanics.
- Implement the Go routine that generates unique multi-sig deposit addresses for BTC and EVM chains. 
- Upon confirmation of locking, the Go backend must cryptographically verify the lock state to authorize the Soroban smart contract to mint the matching 1:1 wrapped asset (e.g., `luBTC`) to the user's Pi Browser wallet. 
- Implement the reverse `Burn()` logic for standard asset release back to external wallets.

#### C. Pure Web3 Financial Services
- **Auto-Compounding Vaults:** Dashboards that automatically harvest and re-stake yields.
- **Liquid Staking Derivatives (LSDs):** Staking `luBTC` issues the user a receipt token (`stluBTC`) that can be used elsewhere in the ecosystem.
- **Cross-Chain Perpetuals & Flash Loans:** Build the UI stubs for uncollateralized flash loans and leveraged margin trading.

### 5. Development Phase Execution Plan

**Phase 1: Cryptographic Core & Backend (Go/Supabase)**
- Establish non-custodial address derivation logic.
- Set up Supabase schemas for `user_profiles`, `user_wallets_mapping`, and `transaction_ledger` (Tracking exact states: `Pending_Deposit`, `Locked_on_Source`, `Minting_luAsset`, `Complete`).
- Set up background workers in Go to index external RPC nodes.

**Phase 2: Core Financial Logic (Backend/Smart Contract Stubs)**
- Build the math for the Dynamic Utilization Curve.
- Program all categorized fees (Bridging, Borrowing, Staking, Maker/Taker) to funnel exclusively into the designated Pi Developer Wallet.
- Integrate the Reflector Oracle price feed simulations to calculate Loan-to-Value (LTV) and liquidation thresholds.

**Phase 3: The 3D Glassmorphic Frontend (Next.js/Three.js)**
- Construct the terminal dashboard layout using the #0A0A0F and #F0C040 palette.
- Implement the interactive 3D spatial typography and WebGL visualizers.
- Build the `DepositModal` featuring real-time state pipeline tracking and passkey biometric signing simulations.
- **Dashboard Structure:** View 1: "Overview" (Total Value Locked, 3D Yield Visualizer); View 2: "Wallets" (Multi-chain Address Generation); View 3: "Vaults" (Staking/Borrowing interfaces featuring the Dynamic Rate curve graph).

Please generate the complete, fully integrated workspace structure with clean, production-ready component code covering these exact specifications.
